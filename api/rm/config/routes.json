{
  "routes": [
    {
      "method": "GET",
      "path": "/rms",
      "handler": "rm.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/rms/count",
      "handler": "rm.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/rms/:id",
      "handler": "rm.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/rms",
      "handler": "rm.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/rms/:id",
      "handler": "rm.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/rms/:id",
      "handler": "rm.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
