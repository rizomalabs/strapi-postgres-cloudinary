{
  "routes": [
    {
      "method": "GET",
      "path": "/report",
      "handler": "report.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/report",
      "handler": "report.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/report",
      "handler": "report.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
